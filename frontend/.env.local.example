# ─────────────────────────────────────────────
# .env.local.example
# このファイルを .env.local にコピーして使う
# .env.local は .gitignore に追加済みのため Git にコミットされない
# ─────────────────────────────────────────────

# FastAPI のベースURL
# 開発時: http://localhost:8000
# 本番時: https://tech0-search-api.azurewebsites.net（APIデプロイ後に変更）
NEXT_PUBLIC_API_URL=http://localhost:8000
